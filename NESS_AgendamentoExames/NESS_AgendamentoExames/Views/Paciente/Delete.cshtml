@model NESS_AgendamentoExames.Models.Paciente

@{
    ViewData["Title"] = "Apagar Paciente";
}

<h4>Você tem certeza que deseja apagar o paciente abaixo?</h4>
<br />

<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Nome)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Nome)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="submit" value="Sim" class="btn btn-danger" /> | <a asp-action="Index">Voltar</a>
    </form>
</div>
